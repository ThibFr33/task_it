  <div class= "my-title">
    <h2>Mes listes</h2>
  </div>
    <ul><% @lists.each do |list| %></ul>
      <%= link_to list_path(list), class:"lists-card-link" do %>
        <div class="lists-card-index">
          <%= list.name %>
        </div>
      <% end %>
    <% end %>
