<%=link_to list_path(list), class: "col-6 p-0 text-decoration-none" do %>
      <div class= "list-card-dashboard" style="background-image: url(<%= image_path(image_category(list.category)) %>)">
        <p><%= list.name  %></p>
      </div>
<% end %>
